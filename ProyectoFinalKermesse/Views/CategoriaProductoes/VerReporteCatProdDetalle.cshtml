@model IEnumerable<ProyectoFinalKermesse.Models.CategoriaProducto>

@{
    ViewBag.Title = "VerReporteCatProdDetalle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>VerReporteCatProdDetalle</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCatProd }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCatProd }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCatProd })
        </td>
    </tr>
}

</table>
