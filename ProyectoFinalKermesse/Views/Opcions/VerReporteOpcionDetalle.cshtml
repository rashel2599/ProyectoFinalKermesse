@model IEnumerable<ProyectoFinalKermesse.Models.Opcion>

@{
    ViewBag.Title = "VerReporteOpcionDetalle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>VerReporteOpcionDetalle</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.opcionDescripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.opcionDescripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idOpcion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idOpcion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idOpcion })
        </td>
    </tr>
}

</table>
